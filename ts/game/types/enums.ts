export enum GameKey {
	UP = 38,
	DOWN = 40,
	LEFT = 37,
	RIGHT = 39,
	JUMP = 32
}

export enum ScreenEdge {
	NORTH,
	SOUTH,
	EAST,
	WEST
}    

export enum Direction {
	NONE,
	UP,
	DOWN,
	LEFT,
	RIGHT
}

export enum Speed {
	SLOW,
	NORMAL,
	FAST
}